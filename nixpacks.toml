[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311.pkgs.pip"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:${PORT:-5000} --workers=1 --threads=4 --timeout=120"
